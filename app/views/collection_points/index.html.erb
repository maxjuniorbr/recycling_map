<div class="container">
  <% if notice.present? %>
    <div class="row">
      <div class="col s12">
        <div class="card-panel green lighten-2">
          <span class="white-text"><%= notice %></span>
        </div>
      </div>
    </div>
  <% end %>
  <div class="row">
    <div class="col s12">
      <br/>
      <nav>
        <div class="nav-wrapper">
          <a href="#" class="brand-logo center">Collection Points</a>
        </div>
      </nav>
      <table class="striped responsive-table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Address</th>
            <th>Contact</th>
            <th>Website</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @collection_points.each do |collection_point| %>
            <tr id="<%= dom_id collection_point %>">
              <td><%= collection_point.name %></td>
              <td><%= collection_point.address %></td>
              <td><%= collection_point.contact %></td>
              <td><%= collection_point.website %></td>
              <td><%= link_to 'Show', collection_point, class: "btn" %></td>
              <td><%= link_to 'Edit', edit_collection_point_path(collection_point), class: "btn" %></td>
              <td><%= link_to 'Destroy', collection_point, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn red darken-1" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <br/>
      <%= link_to "New", new_collection_point_path, class: "btn" %>
    </div>
  </div>
</div>
